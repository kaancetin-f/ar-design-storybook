import { Title, Canvas, Meta, Stories, Controls } from "@storybook/addon-docs/blocks";

import * as ButtonActionStories from "./ButtonAction.stories";

<Meta of={ButtonActionStories} />

<Title />

```typescript
import { ButtonAction } from "@harjs/react-ui";
```

## Ne zaman kullanılır?

Buton, bir eylemi tetiklemek veya bir formu göndermek için kullanılan etkileşimli bir öğedir.
Kullanıcıların tek bir tıklamayla aksiyon almasını sağlamak için butonları kullanın.

#### Canlı Önizleme

<Canvas of={ButtonActionStories.Editor} />

Tabloda yer alan herhangi bir parametrenin değerini değiştirdiğinizde, yukarıdaki canlı önizleme alanı (Canvas) kod seviyesinde yeniden derlenerek anlık çıktıyı gösterir.

<Controls />
